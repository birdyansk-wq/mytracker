# Трекер привычек — Telegram-бот

Ежедневный трекер привычек и состояния для одного пользователя.

## Структура проекта

```
/bot
  main.py      — точка входа, обработчики
  config.py    — конфигурация
  database.py  — SQLite
  scheduler.py — расписание опросов
  questions.py — тексты вопросов
.env          — токен и user_id (создать из .env.example)
requirements.txt
```

## Установка

1. Клонировать/скачать проект
2. Создать виртуальное окружение:
   ```bash
   python -m venv venv
   source venv/bin/activate   # Linux/macOS
   # или venv\Scripts\activate на Windows
   ```
3. Установить зависимости:
   ```bash
   pip install -r requirements.txt
   ```
4. Создать бота в [@BotFather](https://t.me/BotFather), получить токен
5. Узнать свой ID через [@userinfobot](https://t.me/userinfobot)
6. Скопировать `.env.example` в `.env` и заполнить:
   ```
   BOT_TOKEN=ваш_токен
   ALLOWED_USER_ID=ваш_telegram_id
   ```

## Запуск

Из корня проекта:

```bash
python3 -m bot.main
```

## Первый запуск (Онбординг)

При первом запуске бота командой `/start` происходит умная настройка:

1. **Показывается текущая дата и день недели**
2. **Задачи на сегодня** — вводишь список дневных задач
3. **Цели на неделю:**
   - Пн-Чт → устанавливаются на текущую неделю
   - Пт-Вс → устанавливаются на следующую неделю
4. **Цели на месяц:**
   - ≥3 дня до конца месяца → на текущий месяц
   - <3 дня → на следующий месяц

После настройки бот сразу запускает утренний опрос и начинает работу!

В конце онбординга появляется информация о **Mini App** — кнопке «Открыть» рядом с полем ввода, где всегда доступно меню с командами и расписанием.

## Mini App (полноценный интерфейс)

В папке `webapp/` — полноценное веб-приложение с 4 экранами. Дизайн следует [гайдлайнам Telegram](https://core.telegram.org/bots/webapps).

### Экраны Mini App:
1. **🎯 Цели** — управление дневными, недельными и месячными целями
2. **📊 Прогресс** — статистика по всем показателям за неделю
3. **🍷 Алкоголь** — дни без алкоголя и экономия денег (крупно)
4. **⚙️ Настройки** — редактирование вопросов, сброс данных, помощь

### Запуск API для Mini App:

Mini App работает через простой Flask API. Нужно запустить оба процесса:

**Терминал 1 — Бот:**
```bash
python3 -m bot.main
```

**Терминал 2 — API:**
```bash
python3 -m bot.api
```

API слушает на `http://localhost:5000`. Для локальной разработки можно использовать ngrok.

### Деплой (например, Vercel):

**Фронтенд (webapp):**
1. Создай проект на [vercel.com](https://vercel.com)
2. Подключи репозиторий, укажи `webapp` как корневую папку
3. После деплоя добавь в `.env`:
   ```
   WEBAPP_URL=https://твой-проект.vercel.app
   BOT_USERNAME=имя_твоего_бота
   ```

**Backend (API):**
- Деплой Flask API на отдельный хостинг (Railway, Render, Heroku)
- Обнови `API_BASE_URL` в `webapp/app.js` на адрес твоего API

4. Перезапусти бота — появится синяя кнопка **«Открыть»** рядом с полем ввода

Без деплоя Mini App бот работает как раньше, кнопка просто не появится.

## Функционал

### Ежедневные задачи
- **Каждое утро (9:00):** бот спрашивает список задач на день
- **В течение дня:** отмечаешь выполнение через `/today_goals` (чекбоксы)
- **Автоматический респект** при выполнении всех задач

### Ежедневные опросы
- **Утренний опрос (9:00 по Красноярску):** время пробуждения, был ли алкоголь вчера
- **Вечерний опрос (21:00):** прогулка, энергия 1–10

### Еженедельное планирование
- **Понедельник 9:00:** вопрос "Какие цели на неделю?" — вводишь список задач
- **В течение недели:** отмечаешь выполнение через `/goals` (чекбоксы)
- **Пятница 22:00:** проверка целей
  - Все выполнены → респект 🔥
  - Есть невыполненные → предложение перенести на следующую неделю
- **Автоматический респект:** если все задачи выполнены раньше пятницы

### Ежемесячное планирование
- **Первое число месяца 9:00:** вопрос "Какие цели на месяц?" — вводишь список задач
- **В течение месяца:** отмечаешь выполнение через `/month_goals` (чекбоксы)
- **Последний день месяца 22:30:** проверка целей
  - Все выполнены → респект 🔥
  - Есть невыполненные → предложение перенести на следующий месяц
- **Автоматический респект:** если все задачи выполнены раньше конца месяца

### Команды
- **`/start`** — при первом запуске: онбординг; при повторном: показ прогресса
- **`/today`** — ответы за сегодня (с дневными задачами)
- **`/week`** — полная статистика за 7 дней (алкоголь, энергия + прогресс по всем целям)
- **`/today_goals`** — задачи на сегодня с чекбоксами ☑️
- **`/goals`** — цели на неделю с чекбоксами ☑️
- **`/month_goals`** — цели на месяц с чекбоксами ☑️
- **`/questions`** — настроить вопросы (интерактивное меню)
- **`/test`** — симуляция полного цикла бота
- **`/reset`** — полностью сбросить все данные и начать заново

### Еженедельная сводка
- **Воскресенье 14:00:** автоматическая сводка с финансами по алкоголю и прогрессом по целям

**Редактирование вопросов:**
1. Отправь `/questions`
2. Нажми на кнопку с нужным вопросом
3. Выбери "📝 Изменить текст" или "🔘 Изменить варианты"
4. Отправь новый текст или варианты

Данные хранятся в `data/habits.db` (SQLite). Вопросы тоже в БД — изменения применяются сразу.

## Финансовая модель

Финансовая логика основана на сравнении с базовой **недельной нормой 7,000 ₽** (≈30,000 ₽/месяц).

- Каждый эпизод алкоголя = 3,000 ₽
- План на неделю = 7,000 ₽
- Разница = План - Факт

Бот показывает:
- **💰 Экономия** — если потратил меньше плана
- **⚠️ Перерасход** — если потратил больше плана
- **✅ По плану** — если точно по бюджету

Это просто бухгалтерия, без оценок и морали.
